# software architecture

- Dependency Inversion Principle:
  - Structure code so that low-level modules depend on abstractions, not the other way around.
  - This prevents outer-level components like UI or databases from impacting the core business logic.
