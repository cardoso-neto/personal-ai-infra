# `mp600-4tb` setup journal

Operational pointers: [mp600-4tb.md](mp600-4tb.md).

## 2026-07-22: Ethernet stability

Installed `disable-eee-enp5s0.service` to run `ethtool --set-eee enp5s0 eee off` when the interface appears, preventing Realtek r8169 link flapping.
History also records a temporary fixed 100-Mbit test, then restoration of autonegotiation.
On 2026-09-11 the unit was enabled, EEE was disabled, and the link used autonegotiation at 1 Gbit/s.

## By 2026-08-11: T3 setup

The previous runbook records installing Node under `~/.local/opt`, with a stable `node-v24` selector and commands in `~/.local/bin`.
Global npm packages use `~/.local`; `~/.npmrc` permits the `node-pty` and `msgpackr-extract` install scripts needed by T3.
T3's installer created the user systemd service; linger supports boot startup without login.
T3 Connect was authorized for remote access through a loopback listener.

## 2026-09-09: T3 update

Eternal Bash history records `npm install -g --prefix "$HOME/.local" t3@latest`.
It does not establish whether the service runtime was updated afterward.

## 2026-09-11: Documentation audit

Checked both Bash histories and current configuration; original T3 provisioning was absent from the available history.
Confirmed the user service and linger, Node paths, and npm script permissions.
Agent-log collection has a user timer; other scheduled tasks are discoverable in the user crontab.
The shared `~/.agents` link was absent.
