repos:
  - repo: local
    hooks:
      - id: check-symlinks
        name: Check staged symlinks
        entry: uv run --no-project python scripts/check-symlinks.py
        language: system
        always_run: true
        pass_filenames: false
